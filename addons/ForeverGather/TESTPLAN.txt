FOREVER GATHER RC1 — 15 MINUTE CLASSIC ERA TEST
================================================

0. Replace the old ForeverGather folder completely.
1. In game: /console scriptErrors 1
2. /reload
3. /fg

A. UI SMOKE TEST
- /fg test mine
- /fg test herb
- /fg test skin
Expected: three learned records, toast, minimap pins, World Map pins.
- /fg hud
Expected: premium radar opens and pins are visible.
- /fg route
Expected: route builds if two or more visible test/real locations exist.

B. MINING
- Mine 3 real veins.
Expected: each successful loot creates/confirms exactly one Mining location.
- Walk away, return, mine the same spawn later.
Expected: observation count increases rather than creating a duplicate nearby.

C. HERBALISM
- Gather 3 real herbs.
Expected: same behavior as Mining, correct herb names where the client supplies them.

D. SKINNING
- Kill, loot and skin 3 beasts.
Expected: Skinning locations are recorded only after the skinning loot window.
- Open World Map.
Expected: Skinning appears as heat-density visualization when enabled.

E. PERSISTENCE
- /reload
Expected: all learned locations remain.
- Log out/in.
Expected: data remains and previous session is archived.

F. DIAGNOSTICS
- /fg diag
Expected: Recorded counter rises after successful gathers and Invalid = 0.
If a real gather fails to record, screenshot the entire Diagnostics window immediately after the attempt.

G. OPTIONAL INTEGRATIONS
- With GatherMate2 loaded: /fg import gathermate
Expected: imported points appear in the correct zones and do not create impossible coordinates.
- With TomTom loaded: build route, then /fg route next
Expected: a TomTom waypoint is created for the current NEXT stop.
